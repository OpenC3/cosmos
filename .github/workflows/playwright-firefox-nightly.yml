# Copyright 2026 OpenC3, Inc.
# All Rights Reserved.
#
# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.
# See LICENSE.md for more details.

# This file may also be used under the terms of a commercial license
# if purchased from OpenC3, Inc.

name: Playwright Firefox Nightly Tests

on:
  # TODO: Disabling the schedule for now because all the tests with multiline string inputs are broken
  # (e.g. SR 'displays the call stack'). It seems like CRLFs are making it into the test runner somehow, but I can't
  # figure out why. A single string literal like "line 1\nline 2\nline 3" is the only way I can get these tests to pass
  # on Firefox, but that's ugly and makes our tests way less readable. We need to figure out where the CRLFs are coming
  # from, or if something else is breaking multiline input in Firefox.
  #
  # schedule:
  #   - cron: '0 6 * * *'  # Run at 06:00 UTC (midnight MDT) every day
  workflow_dispatch: # Allow manual triggering

permissions:
  contents: read

jobs:
  openc3-firefox-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    permissions:
      contents: read
      packages: write # required to push the GHCR build cache
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          egress-policy: audit

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 24
      - uses: ./.github/actions/build-cosmos
      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
        with:
          version: 11
      - uses: ./.github/actions/setup-playwright
      - name: Check for test.only
        run: pnpm playwright test --list --forbid-only
        working-directory: playwright
      - name: openc3.sh run
        # This `shell` line is required to get around a known issue: https://github.com/actions/runner/issues/241#issuecomment-745902718
        shell: 'script -q -e -c "bash {0}"'
        run: ./openc3.sh run
        # Allow the containers to spin up
      - name: Sleep for 2min
        run: sleep 120s
        shell: bash
        # Check container status
      - name: Check container status
        shell: 'script -q -e -c "bash {0}"'
        run: |
          docker ps
          docker logs cosmos-openc3-cosmos-init-1
          docker logs cosmos-openc3-operator-1
          docker logs cosmos-openc3-cosmos-cmd-tlm-api-1
          docker logs cosmos-openc3-cosmos-script-runner-api-1
        # Build a test plugin for playwright and a copy so we can 'upgrade'.
        # The gems are a pure function of the plugin/target templates and the
        # generator, so cache them rather than paying ~1.5 min of container
        # startup and `gem build` on every run.
      - name: Cache test plugin gems
        id: pw_test_plugin
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          path: playwright/openc3-cosmos-pw-test
          key: pw-test-plugin-${{ hashFiles('openc3/templates/plugin/**', 'openc3/templates/target/**', 'openc3/lib/openc3/utilities/cli_generator.rb', 'playwright/playwright.sh') }}
      - name: Build plugin
        if: steps.pw_test_plugin.outputs.cache-hit != 'true'
        shell: 'script -q -e -c "bash {0}"'
        run: ./playwright.sh build-plugin
        working-directory: playwright
        # No separate "Wait for build" step: the firefox project depends on the
        # 'setup' project (tests/global.setup.ts), which waits for the interfaces
        # to connect and takes the demo sim out of QUIET mode on every invocation.
      - name: Run Playwright Firefox tests
        run: |
          pnpm playwright test --project=firefox ./tests/**/*.p.spec.ts --grep-invert='enterprise'
          pnpm playwright test --project=firefox ./tests/**/*.s.spec.ts --workers=1 --grep-invert='enterprise'
        working-directory: playwright
      # No coverage upload: coverage is collected via Chromium's V8 profiler
      # (see playwright/tests/fixture.ts), which Firefox does not support.
      - name: Upload test artifacts
        if: always() # Upload on pass or fail
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: playwright-firefox
          path: playwright/test-results
          retention-days: 30
